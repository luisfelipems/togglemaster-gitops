apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: togglemaster
spec:
  selector:
    app: flag-service
  ports:
    - port: 80
      targetPort: 8002
      protocol: TCP
      name: http
  type: ClusterIP