apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: togglemaster

spec:
  selector:
    app: auth-service

  ports:
    - name: http
      port: 80
      targetPort: 8001
      protocol: TCP
      

  type: ClusterIP # Acessível apenas dentro do cluster