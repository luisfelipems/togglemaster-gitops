apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: togglemaster
spec:
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 80
      targetPort: 8005
      protocol: TCP
  type: ClusterIP